export default defineNuxtRouteMiddleware((to, from) => {
   // const isLoggedIn = true; // Replace with actual authentication check logic
    //if (isLoggedIn) {
        //return navigateTo('custom'); // Redirect to login page if not authenticated
    //}
   // return navigateTo('/')
  // Middleware logic to check authentication
  //const isAuthenticated = useAuth().isAuthenticated();

  //if (!isAuthenticated) {
    // Redirect to login page if not authenticated
  //  return navigateTo('/login');
  //}

  // Optionally, you can add more logic here, like checking user roles or permissions
});